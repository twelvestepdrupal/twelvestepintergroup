<?php

/**
 * @file
 * Contains anongroups.module..
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function anongroups_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the anongroups module.
    case 'help.page.anongroups':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Anonymous 12 Step Group Entity') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function anongroups_theme() {
  $theme = [];

  return $theme;
}
