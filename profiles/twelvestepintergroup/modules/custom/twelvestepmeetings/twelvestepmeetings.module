<?php

/**
 * @file
 * Contains twelvestepmeetings.module..
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function twelvestepmeetings_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the twelvestepmeetings module.
    case 'help.page.twelvestepmeetings':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Twelve Step Meetings Entity') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function twelvestepmeetings_theme() {
  $theme = [];

  return $theme;
}

/**
 * Implements hook_library_info_alter().
 */
function twelvestepmeetings_library_info_alter(&$libraries, $extension) {
  if ($extension == 'geolocation') {
    // Replace the geolocation JS with our own.
    $old_path = 'js/geolocation-common-map.js';
    unset($libraries['geolocation.commonmap']['js'][$old_path]);
    $base_module = base_path() . drupal_get_path('module', 'twelvestepmeetings');
    $libraries['geolocation.commonmap']['js']["$base_module/$old_path"] = [];
    $libraries['geolocation.commonmap']['css']['theme']["$base_module/css/meetings.css"] = [];
  }
}
